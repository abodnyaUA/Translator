// Test Application

// Header
@interface SampleCode
	int a,bcb

// Body
@implementation
	input(a)
	if a>=3 and a<=10
		a = a + 5
	else
		output(a)
	endif
	for bcb = 1^75 to 5 step 1
	{
		a = a * bcb
	}
	next
@end